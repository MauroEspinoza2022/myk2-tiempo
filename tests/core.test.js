import {test} from 'node:test';import assert from 'node:assert/strict';import {extraMinutes,limaNow,totals,grouped,dayEquivalent,validDate} from '../src/core.js';
test('Hora de Lima independiente del dispositivo',()=>assert.deepEqual(limaNow(new Date('2026-09-21T02:30:00Z')),{date:'2026-09-20',time:'21:30'}));
test('Salidas lunes, viernes y sábado; sin redondeos',()=>{for(const [expected,end,n]of [['16:00','18:15',135],['17:00','18:15',75],['12:00','15:20',200],['17:00','16:00',0]])assert.equal(extraMinutes({kind:'ordinary',expected,end}),n);});
test('Domingos, pausa y cruce de medianoche',()=>{assert.equal(extraMinutes({kind:'restday',start:'07:00',end:'12:00',pause:30}),270);assert.equal(extraMinutes({kind:'ordinary',expected:'17:00',end:'01:30',nextDay:true}),510);assert.throws(()=>extraMinutes({kind:'restday',start:'07:00',end:'06:00'}));});
test('No acepta pausas negativas ni fechas imposibles',()=>{assert.throws(()=>extraMinutes({kind:'ordinary',expected:'17:00',end:'18:00',pause:-1}));assert.equal(validDate('2026-02-30'),false);});
test('Descansos solicitados no descuentan, aprobados reservan y gozados descuentan una vez',()=>{assert.deepEqual(totals([{minutes:1200}],[{minutes:100,status:'requested'},{minutes:480,status:'approved'},{minutes:240,status:'taken'}]),{earned:1200,used:240,reserved:480,balance:960,available:480});});
test('Conversión y agrupación semanal lunes a domingo',()=>{assert.deepEqual(dayEquivalent(990),{days:2,remainder:30,decimal:2.0625});assert.deepEqual(grouped([{date:'2026-09-20',minutes:60},{date:'2026-09-21',minutes:30}],[],'week').map(x=>x.period),['2026-09-14','2026-09-21']);});
